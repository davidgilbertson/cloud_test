{
  "name": "cloud_test",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build",
    "dev:cf": "vite",
    "dev:fb": "npm --prefix functions run build && npm run build && firebase emulators:start --only hosting,functions,firestore",
    "deploy:cf": "npm run build && wrangler deploy",
    "deploy:fb": "npm run build && firebase deploy --only hosting,functions"
  },
  "dependencies": {
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@cloudflare/vite-plugin": "^1.15.2",
    "@tailwindcss/vite": "^4.1.17",
    "@vitejs/plugin-react": "^5.1.1",
    "tailwindcss": "^4.1.17",
    "typescript": "^5.7.3",
    "vite": "^7.2.4",
    "wrangler": "^4.50.0"
  }
}
